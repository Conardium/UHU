
   					--------- PRACTICA 9 ---------

1) sudo cp /etc/default/grub /etc/default/grub_original


2) sudo joe /etc/default/grub
   	#Modificamos el archivo con:
   	GRUB_TIMEOUT_STYLE=menu
   	GRUB_TIMEOUT=60
  	GRUB_DISABLE_RECOVERY="false"
  	GRUB_GFXMODE=800x600
   	GRUB_INIT_TUNE="480 440 1"
    sudo chmod a-x /etc/grub.d/20_memtest86+
    sudo update-grub

3) sudo reboot


4) sudo mkdir /boot/kernels
   sudo mv /boot/vmlinuz-5.4.0-42-generic /boot/kernels
   sudo mv /boot/initrd.img-5.4.0-42-generic /boot/kernels
   sudo update-grub
   sudo reboot


5) dmseg | grep EXT4


6) systemctl --type=service --state=active


7) journalctl -u NetworkManager | grep Error


8) systemctl get-default


9) sudo apt-get install ssh


10) systemctl status ssh
    ssh localhost


11) systemctl status ssh


12) cat /lib/systemd/system/ssh.service
    #Se reinicia en caso de fallo (on-failure)


13) sudo systemctl stop ssh.service   
    //Es lo mismo que: sudo systemctl stop ssh

 
14) sudo systemctl disable ssh.service


15) sudo systemctl mask ssh


16) systemctl status ssh
    #enmascarado, inactivo y deshabilitado


17) sudo systemctl unmask ssh
    sudo systemctl start ssh


18) sudo systemctl reeanble ssh


19) pstree -p


20) sudo joe /etc/cron.hourly/repetidor
    	#!/bin/sh
        echo "Ejecutando cron a las $(date)" >>/home/ismael/cadahora
    sudo chmod a+x /etc/cron.hourly/repetidor


21) sudo apt-get install at


22) date


23) sudo chmod a+w cadahora
    at now + 4 minutes
     echo "linea generada por la orden at a las $(date)"


24) cp /etc/cron.hourly/repetidor /home/ismael/programado


25) at -f programado now + 15 minutes


26) atq


27) atrm 2
    atq


28) sudo shutdown 3 "Guardad y salid que esto se va a apagar"


